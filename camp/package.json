{
  "name": "camp",
  "scripts": {
    "postinstall": "spago install && spago build",
    "release": "spago build && vite build",
    "build": "spago build",
    "dev": "node --stack-size=9200 ./node_modules/.bin/vite dev",
    "fmt": "purs-tidy format-in-place 'src/**/*.purs'"
  },
  "browserslist": "defaults, not ie <= 11",
  "devDependencies": {
    "jsdom": "^25.0.1",
    "jsdom-global": "^3.0.2",
    "purescript": "^0.15.15",
    "purs-backend-es": "^1.4.2",
    "purs-tidy": "^0.11.0",
    "spago": "^0.93.41",
    "vite": "^6.0.6"
  }
}
